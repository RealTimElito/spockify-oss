# fixtures-20b-v1

Small `runHarness` stub for gpt-oss-20b. Phase 5 stays Partial — do not claim Done.

## prose-instead-of-tool

Directory: `prose_instead/`

Goal: the model writes a tutorial instead of calling `apply_patch` or `write_file`.

Do not run this pack unless eval GPU is free (eval host / twin).
