# 06 — tip 54 pytest-fail nudge + regress suite

## Tip 54 (harness)
- After red pytest (ImportError/collect or FAILED): one harness nudge in observations
- Cap extra tool rounds after nudge + WRITE when still failing (tool_i ≥ 2)
- `exec_stream_should_stop(..., pytest_goal=True)` waits for pytest RUN before stop

## Tip 54 micro (Spark, post tip-53 think=low)
- hello inspect: **~5.4s**, review skip done
- docs API.md: **~5.3s**, WRITE ok
- Spark prod **1/1** router/OWUI/gateway/XTTS

## Tip 55 lite
- `scripts/lab_regress_quick.sh` — rate + hello + docs one-shot

## Cursor parity (from gap scan)
- Review phase bar: “N files pending · Accept all”
- Live thought: “Wrapping up…” phase label wired
