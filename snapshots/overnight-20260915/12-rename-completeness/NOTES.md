# 12 — tip 60 rename completeness + calc/invent micros

## Tip 60 (on tip 59)
- `parse_rename_pair` + `rename_write_quality_note` nudge when `def old` remains
- Compose rename tip60b: nudge → WRITE core.py+app.py → hello everywhere
- calc **~6s** 2 passed; invent slug **~16s** 2 passed

## Do not undo
54 docs quality gate pattern; 27 rename bootstrap.
