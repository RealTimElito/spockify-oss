# spockify-chat-panel (optional package)

**M0 status:** Chat UI lives in the extension for parallel-safe ownership:

- Host: `extensions/spockify/src/chat/`
- Webview: `extensions/spockify/media/chat/`

This package directory is reserved for a later extract (shared React/webview bundle) per `docs/SPOCKIFY_DESKTOP_IDE_PLAN.md` directory map. Do not put a competing UI here in M0.

See `extensions/spockify/src/chat/README.md` for v1 subset vs deferred (§7) and integration points.
