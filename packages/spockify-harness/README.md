# @spockify/harness

Shared coding-agent kernel for Spockify CLI, IDE, and lab.

Entry point: `runHarness({ transport, session, tools, policy, signal, onEvent })`.

See `docs/HARNESS.md`. Until Phase 5, SWE scored rows may still use mini-SWE-agent;
do not claim harness wins without a kernel SHA on the card.
