# Tab training deps (host venv or NGC training container).
#
# On aarch64 cluster nodes: prefer the NGC vLLM image's torch, or an
# NGC PyTorch container with matching GPU kernels. Stock PyPI torch may lack them.
torch>=2.4.0
transformers>=4.46.0
datasets>=3.0.0
peft>=0.13.0
trl>=0.12.0
accelerate>=1.0.0
safetensors>=0.4.0
psycopg[binary]>=3.2.0
huggingface_hub>=0.26.0
sentencepiece>=0.2.0
protobuf>=4.25.0
kubernetes>=31.0.0
